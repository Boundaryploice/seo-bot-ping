# seo-bot-ping (Render)

Web service: README pixel + npm postinstall ping → Telegram.

Deploy on [Render](https://render.com): **New → Blueprint** → this repo, or connect existing `seo-bot-ping` service to this repository and **Manual Deploy**.

Health check: `/health` should return `ok 2026-05-31-inject`.
